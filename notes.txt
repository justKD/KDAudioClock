bugs:

- in suspend example, if you suspend -> pause -> reschedule -> start, 
the events don't restart correctly

todo:

check/clean up inline documentation 
  - update descriptions with text from readme api section
complete unit tests
add build